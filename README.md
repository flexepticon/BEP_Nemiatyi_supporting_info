This section contains code and raw data for the project.

Due to file size constraints, the files were heavily compressed. Thus, after unarchiving BEP_workingdir folder, one shall also unzip all folders inside. 

The contents of 'working_folders' directory must be transferred to the root 'BEP_workingdir' folder.
